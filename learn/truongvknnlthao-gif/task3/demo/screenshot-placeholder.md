# Demo Screenshots

本目录已包含 Task 3 demo 截图：

- `demo-initial.png`：初始页面。
- `demo-allocation-preview.png`：钱包演示连接后，输入 Scout / Shield / Research 私有分配并生成 `private_allocation_demo.aleo/create_allocation` 调用预览。

截图展示：

1. 页面标题 `Private Allocation Proof`。
2. 钱包状态为已连接。
3. Aleo 地址输入框。
4. 选项 A/B。
5. `private_allocation_demo.aleo/create_allocation` 调用预览结果。

截图不包含 private key、view key、API key 或任何敏感钱包信息。
