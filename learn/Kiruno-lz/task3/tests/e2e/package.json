{
  "name": "aleo-pii-protocol-e2e-tests",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "L3 Playwright end-to-end journeys for Aleo PII Protocol (create / edit / delete / share / consume)",
  "scripts": {
    "test": "bun x playwright test",
    "test:list": "bun x playwright test --list",
    "install:browsers": "bun x playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/node": "^22.10.1",
    "typescript": "^5.6.3"
  }
}
