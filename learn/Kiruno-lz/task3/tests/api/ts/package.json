{
  "name": "aleo-pii-protocol-api-tests",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "L2 API contract tests for Aleo PII Protocol (cross-language schema + frontend-backend coordination)",
  "scripts": {
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
