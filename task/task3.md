# Task 3 - Aleo SDK 与钱包集成

> 对应章节三：使用 SDK 与 Shield 钱包构建 dApp

请将本文件复制到 `learn/YourName/` 文件夹中，填写你的答案后提交 PR。


## 问题

**Q1. 如何使用 SDK 命令生成一个全新的随机 Aleo 账户？如果你只有 PrivateKey，该如何重建该账户？**

A:

---

**Q2. 使用 ProgramManager 部署程序时需要 NetworkConfig。你该如何指定要广播部署的 Aleo 节点 URL？**

A:

---

**Q3. 使用 execute 和 run 之间有什么区别？**

A:

---

**Q4. Aleo 程序使用特定类型（例如 1u32, 100field）。SDK 要求在向函数调用传递这些输入参数组时采用什么格式？**

A:

---

**Q5. 使用 Aleo Wallet Adapter 的 useWallet 钩子时，如何触发 Shield 钱包扩展弹出并请求用户连接？**

A:

---

**Q6. 如果不使用 SDK 的 ProgramManager，如何将交易请求委托给 Shield 钱包，以便用户可以手动审核并签名？**

A:

---

**Q7. 如何使用 requestRecords 方法从 Shield 钱包获取用户的未花费记录（unspent records）？获取后如何按特定程序名称进行筛选？**

A:

---

**Q8. 你想让用户登录你的网站。如何使用 Shield 钱包对"登录我的 dApp"消息进行签名，并在后端验证该签名？**

A:

---

**Q9. 在通过 Shield 钱包调用 requestTransaction 后，它会返回一个交易 ID。你应该轮询哪个 API v2 接口来向用户显示"待处理"或"成功"状态？**

A:

---

**Q10. 如果用户声称拥有积分但你的 dApp 无法显示，你该如何使用 API 检查程序的 mapping 以验证公共变量的当前状态？**

A:
